Audit 9 masterclass skills against the skill-creator guidelines and produce a structured review report.

READ FIRST:
- `.cc-dispatch/loops/loop-018-skill-audit/goal.md` — full spec.
- `.cc-dispatch/loops/loop-018-skill-audit/references/` — INDEX.md, then everything it lists (this is your rubric).

Mode: autonomous (`--dangerously-skip-permissions`).
Audit the skills in `skills/` (excluding `skill-creator`). Do NOT modify any skill files.
Evaluate strictly against the provided reference docs.

Done when: A comprehensive `report.md` is written to the loop folder containing per-skill findings, scores, and concrete recommendations based on the rubric. Or stop after 30 turns if blocked.

Stop after the report is written: write `progress.md` heartbeat and go idle. Do not begin the next loop.