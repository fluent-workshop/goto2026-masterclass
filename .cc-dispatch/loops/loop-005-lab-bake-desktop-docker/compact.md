keep: Path 2 hybrid chosen (extend bash bake, no Ansible); the 4 locked decisions (desktop IN = Xfce+noVNC+nginx-auth, Anthropic org sub-keys, new Discord server, Funnel verified on first box later); ccx33 32GB; loop-005 task = bake desktop+Docker into bootstrap.sh, file-editing only no spend; drop: the full loop-004 divergence-map detail (it's in references/), the FR-by-FR reconciliation reasoning, the rescope-path analysis
