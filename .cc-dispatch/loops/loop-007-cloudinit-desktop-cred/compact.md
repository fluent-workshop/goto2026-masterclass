keep: box #1 live + GREEN (goto-test 87.99.153.105, recipe snapshot-ready, fail-closed); the baked cred contract = cloud-init must write /etc/openclaw/desktop.env with DESKTOP_USER/DESKTOP_PASS; loop-007 = write that write_files block + the missing infra/clone.sh renderer (per-box password gen, gitignored output+manifest), validate on live box 401->200->401, leave fail-closed, NO snapshot/clone/secrets; Funnel deferred (needs Tailscale key from Cedric); drop: the loop-006 bug-fix detail (in references/), the loop-005 authoring history
